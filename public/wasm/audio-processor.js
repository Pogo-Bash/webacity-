// Placeholder for WASM audio processor
// To build the actual WASM module, run: cd src/wasm && ./build.sh

export default function() {
  return Promise.reject(new Error('WASM audio processor not compiled. Run: cd src/wasm && ./build.sh'));
}
